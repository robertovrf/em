//uses rest.RESys
uses monitoring.Perception
uses metric.MetricNotifier

interface Learning {
	void learns(store Perception resys, int collectionTime, int explorationTreshold, int rounds, MetricNotifier notifier)
}
